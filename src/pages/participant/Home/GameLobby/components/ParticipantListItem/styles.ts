import styled from 'styled-components';

export const ParticipantListItemWrapper = styled.li`
  display: flex;
  align-items: center;
  color: #ffffff;
  font-size: 1.6rem;
  font-style: normal;
  font-weight: 400;
  line-height: normal;
  gap: 24px;
  padding: 0 8px;
`;
